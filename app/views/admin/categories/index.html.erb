<table>
	<thead>
		<tr>
			<th>Categories</th>
			<th></th>
		</tr>
	</thead>
<% @categories.each do |category| %>
	<tr>
		<td><%= link_to category.name, admin_category_url(category) %></td>
		<td><%= link_to "Edit", edit_admin_category_url(category)  %> </td>
	</tr>
<% end %>
</table>
<%= link_to "Add Category", new_admin_category_url  %>
